# Scripts

Benchmark runner scripts have moved to per-benchmark directories. Each benchmark now has its own `run.sh`:

```bash
bash benchmarks/aime25/run.sh
bash benchmarks/hmmt25/run.sh
bash benchmarks/gpqa_diamond/run.sh
```

See `benchmarks/README.md` for full details.
